version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd clt
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: clt
    files:
      - '**/*'
  cache:
    paths: []